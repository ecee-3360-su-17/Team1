 .global fib

fib:

base_case_one:
	cmp r0, #0
	bne base_case_two:
	mov r0, #0
	mov pc, lr

base_case_two:
	cmp r0, #1
	bne recurse
	mov r0, #1
	mov pc, lr

recurse:
	push {r0}
	sub r0, r0, #1
	b fib
	mov r3, r0
	pop {r0}
	sub r0, r0, #2
	b fib
	add r0, r0, r3
	mov pc, lr
